version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"

  # Add an entry per package manager used in this repo, for example:
  #
  # - package-ecosystem: npm
  #   directory: /
  #   schedule:
  #     interval: weekly
  #   groups:
  #     minor-and-patch:
  #       update-types:
  #         - minor
  #         - patch
